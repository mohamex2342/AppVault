# 🚀 دليل الإعداد السريع

دليل خطوة بخطوة لإعداد تطبيق المحادثة باستخدام Supabase.

---

## ⚡ الخطوات السريعة

### 1. إنشاء مشروع Supabase

1. اذهب إلى https://supabase.com
2. سجّل الدخول أو أنشئ حساب جديد
3. اضغط على **New Project**
4. املأ البيانات:
   - **Name**: اسم المشروع (مثال: chat-app)
   - **Database Password**: كلمة مرور قوية (احفظها جيداً)
   - **Region**: اختر أقرب منطقة لك
5. اضغط على **Create new project**
6. انتظر حتى يتم إنشاء المشروع (2-3 دقائق)

---

### 2. إعداد قاعدة البيانات

#### أ. افتح SQL Editor
1. من القائمة الجانبية، اختر **SQL Editor**
2. اضغط على **New query**

#### ب. نفّذ سكريبت قاعدة البيانات
1. افتح ملف `supabase-setup.sql` من مجلد المشروع
2. **انسخ كل المحتوى** (Ctrl+A ثم Ctrl+C)
3. **الصق المحتوى** في SQL Editor (Ctrl+V)
4. اضغط على **Run** أو اضغط (Ctrl+Enter)
5. انتظر حتى تظهر رسالة **Success!**

✅ تم إنشاء جميع الجداول والإعدادات بنجاح!

---

### 3. إنشاء Storage Bucket

#### الطريقة الأولى: من لوحة التحكم
1. اختر **Storage** من القائمة الجانبية
2. اضغط على **New bucket**
3. املأ البيانات:
   - **Name**: `chat-files`
   - **Public bucket**: ✅ (ضع علامة صح)
4. اضغط على **Create bucket**

#### الطريقة الثانية: عبر SQL (إذا فشلت الطريقة الأولى)
إذا لم تعمل الطريقة الأولى، ارجع لـ SQL Editor ونفّذ:

```sql
INSERT INTO storage.buckets (id, name, public)
VALUES ('chat-files', 'chat-files', true)
ON CONFLICT (id) DO NOTHING;
```

✅ تم إنشاء Storage Bucket بنجاح!

---

### 4. تفعيل Realtime

1. اختر **Database** من القائمة الجانبية
2. اضغط على **Replication**
3. ابحث عن الجداول التالية وفعّل المفتاح بجانب كل منها:
   - ✅ `profiles`
   - ✅ `conversations`
   - ✅ `conversation_members`
   - ✅ `messages`

✅ تم تفعيل التحديثات الفورية!

---

### 5. الحصول على بيانات الاتصال

1. اختر **Settings** من القائمة الجانبية
2. اضغط على **API**
3. ستجد:
   - **Project URL** (URL المشروع)
   - **anon public** (المفتاح العام)
4. **انسخ هذه البيانات**

---

### 6. تحديث بيانات الاتصال في الكود

#### افتح ملف `js/auth.js`
ابحث عن هذه الأسطر في بداية الملف:

```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
```

**استبدل** البيانات الافتراضية ببياناتك:

```javascript
const SUPABASE_URL = 'https://xxxxxxxxxxxxx.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

**ملاحظة مهمة:** تأكد من وضع البيانات داخل علامات الاقتباس `''`

---

### 7. تشغيل التطبيق

#### الطريقة الأولى: VS Code + Live Server
1. افتح المشروع في Visual Studio Code
2. انقر بزر الماوس الأيمن على `auth.html`
3. اختر **Open with Live Server**

#### الطريقة الثانية: Python
افتح Terminal في مجلد المشروع ونفّذ:
```bash
python -m http.server 8000
```
ثم افتح المتصفح على: http://localhost:8000/auth.html

#### الطريقة الثالثة: Node.js
```bash
npx http-server -p 8000
```
ثم افتح المتصفح على: http://localhost:8000/auth.html

---

## ✅ التحقق من التثبيت

### 1. اختبر التسجيل
- افتح صفحة `auth.html`
- انتقل لتبويب **إنشاء حساب**
- املأ البيانات وأنشئ حساباً تجريبياً
- إذا تم إنشاء الحساب بنجاح، فكل شيء يعمل! 🎉

### 2. اختبر المحادثات
- بعد تسجيل الدخول، ستنتقل لصفحة المحادثات
- اضغط على **محادثة جديدة** (إذا كان هناك مستخدمون آخرون)
- أو أنشئ حساباً آخر في نافذة خفية لاختبار المحادثة

---

## ❗ حل المشاكل الشائعة

### المشكلة 1: "لا يمكن الاتصال بـ Supabase"
**الحل:**
- تحقق من صحة `SUPABASE_URL` و `SUPABASE_ANON_KEY`
- تأكد من وضعها داخل علامات الاقتباس
- تأكد من نسخها بالكامل بدون مسافات زائدة

### المشكلة 2: "خطأ في إنشاء الحساب"
**الحل:**
- تحقق من تنفيذ سكريبت `supabase-setup.sql` بنجاح
- تأكد من إنشاء جدول `profiles`
- راجع تبويب **Logs** في Supabase لمعرفة الخطأ

### المشكلة 3: "لا تظهر الرسائل الجديدة تلقائياً"
**الحل:**
- تأكد من تفعيل Realtime للجداول المطلوبة
- في **Database** > **Replication**
- فعّل: profiles, conversations, conversation_members, messages

### المشكلة 4: "لا يمكن رفع الصور/الملفات"
**الحل:**
- تأكد من إنشاء Storage Bucket باسم `chat-files`
- تأكد من جعله Public
- راجع سياسات الأمان (Policies) في Storage

### المشكلة 5: "الصفحة فارغة/لا تعمل"
**الحل:**
- افتح أدوات المطور (F12)
- راجع تبويب **Console** للأخطاء
- تأكد من وجود جميع الملفات:
  - `js/auth.js`
  - `js/chat.js`
  - `js/media.js`
  - `css/style.css`

---

## 📞 هل تحتاج مساعدة؟

إذا واجهت مشكلة:

1. **راجع Console في المتصفح** (F12 > Console)
2. **راجع Logs في Supabase** (في لوحة التحكم)
3. **تحقق من SQL Editor** (هل نُفّذ السكريبت بنجاح؟)
4. **راجع Storage** (هل Bucket موجود؟)
5. **راجع Database > Replication** (هل Realtime مفعّل؟)

---

## 🎉 تهانينا!

أنت الآن جاهز لاستخدام تطبيق المحادثة!

استمتع بالمحادثات والمجموعات ومشاركة الملفات! 💬✨

---

## 📚 الخطوات التالية

بعد إعداد التطبيق، يمكنك:

1. **تخصيص الألوان** - عدّل متغيرات CSS في `css/style.css`
2. **إضافة مميزات جديدة** - استكشف ملفات JavaScript
3. **نشر التطبيق** - استخدم Vercel أو Netlify للنشر المجاني
4. **دعوة الأصدقاء** - شارك رابط التطبيق!

---

**ملاحظة:** هذا التطبيق تعليمي ومفتوح المصدر. يمكنك تعديله واستخدامه كما تريد! 🚀
