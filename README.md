# 📱 منصة متجر التطبيقات - App Store Platform

منصة متكاملة لتحميل وتوزيع التطبيقات مع لوحة تحكم كاملة للمطورين، مبنية باستخدام **HTML, CSS, JavaScript** و **Supabase** كقاعدة بيانات.

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Status](https://img.shields.io/badge/status-active-success)

---

## 🎯 نظرة عامة

هذه المنصة هي حل شامل يتيح للمطورين رفع وإدارة تطبيقاتهم، وللمستخدمين تصفح وتحميل التطبيقات بسهولة. تم تصميمها بواجهة عربية حديثة وسهلة الاستخدام.

---

## ✨ المميزات الرئيسية

### 🎨 للمستخدمين:
- ✅ تصفح التطبيقات المميزة والأكثر شعبية
- ✅ بحث متقدم وفلترة حسب الفئات
- ✅ عرض تفاصيل كاملة للتطبيقات (وصف، لقطات شاشة، تقييمات)
- ✅ تحميل التطبيقات بنقرة واحدة
- ✅ نظام تقييمات ومراجعات
- ✅ إحصائيات حية (عدد التطبيقات، التحميلات، المستخدمين)

### 👨‍💻 للمطورين:
- ✅ لوحة تحكم شاملة بإحصائيات تفصيلية
- ✅ رفع تطبيقات جديدة (APK/IPA + أيقونة + لقطات شاشة)
- ✅ تعديل وحذف التطبيقات الموجودة
- ✅ رسوم بيانية تفاعلية (Chart.js)
- ✅ إدارة كاملة للتطبيقات مع البحث والفلترة
- ✅ معاينة التطبيقات قبل النشر

### 🔐 نظام المصادقة:
- ✅ تسجيل دخول وإنشاء حساب جديد
- ✅ دعم أنواع مختلفة من المستخدمين (مستخدم عادي، مطور، مدير)
- ✅ إعادة تعيين كلمة المرور
- ✅ حماية المسارات والصفحات

---

## 🗂️ هيكل المشروع

```
app-store-platform/
│
├── index.html                  # الصفحة الرئيسية (عرض التطبيقات)
├── auth.html                   # صفحة تسجيل الدخول/التسجيل
├── app-details.html            # صفحة تفاصيل التطبيق
├── developer-dashboard.html    # لوحة تحكم المطور
├── developer-upload.html       # صفحة رفع تطبيق جديد
├── developer-apps.html         # صفحة إدارة التطبيقات
│
├── css/
│   └── style.css              # ملف الأنماط الرئيسي
│
├── js/
│   ├── config.js              # إعدادات Supabase والمتغيرات العامة
│   ├── auth.js                # إدارة المصادقة والمستخدمين
│   ├── main.js                # الصفحة الرئيسية
│   ├── developer-dashboard.js # لوحة تحكم المطور
│   └── developer-upload.js    # رفع وتعديل التطبيقات
│
└── README.md                  # هذا الملف
```

---

## 🚀 التثبيت والإعداد

### 1️⃣ متطلبات التشغيل
- متصفح ويب حديث (Chrome, Firefox, Safari, Edge)
- حساب على [Supabase](https://supabase.com) (مجاني)

### 2️⃣ إعداد قاعدة البيانات Supabase

#### أ) إنشاء مشروع Supabase:
1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ حساباً جديداً أو سجل دخول
3. أنشئ مشروعاً جديداً
4. احفظ `SUPABASE_URL` و `SUPABASE_ANON_KEY`

#### ب) إنشاء الجداول:

قم بتنفيذ SQL التالي في **SQL Editor** على Supabase:

```sql
-- جدول المستخدمين
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  full_name TEXT NOT NULL,
  role TEXT DEFAULT 'user', -- user, developer, admin
  avatar_url TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- جدول الفئات
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT UNIQUE NOT NULL,
  icon TEXT,
  app_count INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);

-- جدول التطبيقات
CREATE TABLE apps (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL,
  description TEXT,
  version TEXT DEFAULT '1.0',
  category TEXT,
  category_id UUID REFERENCES categories(id),
  platform TEXT DEFAULT 'Android', -- Android, iOS, Both
  icon_url TEXT,
  file_url TEXT,
  file_size BIGINT,
  screenshots TEXT, -- JSON array
  rating NUMERIC(2,1) DEFAULT 0,
  downloads_count INTEGER DEFAULT 0,
  price NUMERIC(10,2) DEFAULT 0,
  website TEXT,
  published BOOLEAN DEFAULT false,
  developer_id UUID REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- جدول التقييمات
CREATE TABLE reviews (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  app_id UUID REFERENCES apps(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  rating INTEGER CHECK (rating >= 1 AND rating <= 5),
  comment TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(app_id, user_id)
);

-- إنشاء فهارس للأداء
CREATE INDEX idx_apps_developer ON apps(developer_id);
CREATE INDEX idx_apps_category ON apps(category_id);
CREATE INDEX idx_apps_published ON apps(published);
CREATE INDEX idx_reviews_app ON reviews(app_id);

-- إدراج بيانات تجريبية للفئات
INSERT INTO categories (name, icon, app_count) VALUES
('ألعاب', 'fas fa-gamepad', 0),
('تعليم', 'fas fa-graduation-cap', 0),
('أعمال', 'fas fa-briefcase', 0),
('إنتاجية', 'fas fa-tasks', 0),
('صحة', 'fas fa-heartbeat', 0),
('ترفيه', 'fas fa-film', 0),
('تواصل اجتماعي', 'fas fa-users', 0),
('أدوات', 'fas fa-wrench', 0);
```

#### ج إعداد Storage Buckets:

1. اذهب إلى **Storage** في لوحة Supabase
2. أنشئ 3 Buckets جديدة:
   - `apps` - لملفات التطبيقات (APK/IPA)
   - `icons` - لأيقونات التطبيقات
   - `screenshots` - للقطات الشاشة

3. اضبط سياسات الوصول:
   - **Public Access** لـ `icons` و `screenshots`
   - **Authenticated Access** لـ `apps`

#### د تفعيل Row Level Security (RLS):

```sql
-- سياسات الأمان لجدول التطبيقات
ALTER TABLE apps ENABLE ROW LEVEL SECURITY;

-- السماح للجميع بقراءة التطبيقات المنشورة
CREATE POLICY "Public apps are viewable by everyone"
  ON apps FOR SELECT
  USING (published = true);

-- السماح للمطورين بإدارة تطبيقاتهم فقط
CREATE POLICY "Developers can insert their own apps"
  ON apps FOR INSERT
  WITH CHECK (auth.uid() = developer_id);

CREATE POLICY "Developers can update their own apps"
  ON apps FOR UPDATE
  USING (auth.uid() = developer_id);

CREATE POLICY "Developers can delete their own apps"
  ON apps FOR DELETE
  USING (auth.uid() = developer_id);

-- سياسات للمراجعات
ALTER TABLE reviews ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Reviews are viewable by everyone"
  ON reviews FOR SELECT
  USING (true);

CREATE POLICY "Authenticated users can create reviews"
  ON reviews FOR INSERT
  WITH CHECK (auth.uid() = user_id);
```

### 3️⃣ تكوين المشروع

افتح ملف `js/config.js` وقم بتعديل القيم التالية:

```javascript
const SUPABASE_URL = 'https://xxxxxxxxxxxxx.supabase.co'; // ضع URL مشروعك
const SUPABASE_ANON_KEY = 'your-anon-key-here'; // ضع المفتاح العام
```

### 4️⃣ تشغيل المشروع

1. قم برفع الملفات على أي خادم ويب (مثل: GitHub Pages, Netlify, Vercel)
2. أو افتح `index.html` مباشرة في المتصفح للتطوير المحلي

---

## 📋 الصفحات المتاحة

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| الصفحة الرئيسية | `/` أو `index.html` | عرض التطبيقات المتاحة |
| تسجيل الدخول | `auth.html` | تسجيل دخول أو إنشاء حساب |
| تفاصيل التطبيق | `app-details.html?id={app_id}` | تفاصيل تطبيق معين |
| لوحة تحكم المطور | `developer-dashboard.html` | إحصائيات ورسوم بيانية |
| رفع تطبيق | `developer-upload.html` | رفع تطبيق جديد |
| تعديل تطبيق | `developer-upload.html?edit={app_id}` | تعديل تطبيق موجود |
| إدارة التطبيقات | `developer-apps.html` | عرض وإدارة جميع التطبيقات |

---

## 🎨 التقنيات المستخدمة

### Frontend:
- **HTML5** - بنية الصفحات
- **CSS3** - التصميم والأنماط
- **JavaScript (ES6+)** - البرمجة والتفاعل
- **Bootstrap 5 RTL** - إطار عمل UI
- **Font Awesome** - الأيقونات
- **Chart.js** - الرسوم البيانية
- **Google Fonts (Cairo)** - خطوط عربية

### Backend:
- **Supabase** - قاعدة بيانات PostgreSQL
- **Supabase Auth** - نظام المصادقة
- **Supabase Storage** - تخزين الملفات
- **Supabase Realtime** - التحديثات اللحظية

---

## 👥 أنواع المستخدمين

### 1. مستخدم عادي (`user`):
- تصفح وتحميل التطبيقات
- كتابة تقييمات ومراجعات
- عرض تفاصيل التطبيقات

### 2. مطور (`developer`):
- كل صلاحيات المستخدم العادي
- رفع وإدارة التطبيقات
- الوصول إلى لوحة التحكم والإحصائيات
- تعديل وحذف تطبيقاتهم الخاصة

### 3. مدير (`admin`):
- كل الصلاحيات السابقة
- إدارة جميع التطبيقات
- إدارة المستخدمين والفئات

---

## 🔧 المميزات المُنفذة

### ✅ تم الانتهاء منها:

1. **نظام المصادقة الكامل**
   - تسجيل دخول/خروج
   - إنشاء حساب جديد
   - إعادة تعيين كلمة المرور
   - إدارة الجلسات

2. **الصفحة الرئيسية**
   - عرض التطبيقات المميزة
   - بحث وفلترة متقدمة
   - إحصائيات حية
   - فئات التطبيقات

3. **صفحة تفاصيل التطبيق**
   - معلومات شاملة
   - لقطات الشاشة
   - التقييمات والمراجعات
   - زر تحميل مع عداد

4. **لوحة تحكم المطور**
   - إحصائيات تفصيلية
   - رسوم بيانية (التحميلات، الفئات)
   - جدول التطبيقات الأخيرة
   - روابط سريعة

5. **صفحة رفع التطبيقات**
   - نموذج شامل
   - رفع ملفات متعدد (Drag & Drop)
   - معاينة الملفات
   - التحقق من صحة البيانات

6. **صفحة إدارة التطبيقات**
   - جدول تفاعلي
   - بحث وفلترة متقدمة
   - تعديل وحذف
   - عرض الإحصائيات

---

## 🚧 تحسينات مستقبلية

### 📝 مقترحة للتطوير:

1. **تكامل Storage فعلي**
   - رفع الملفات إلى Supabase Storage
   - شريط تقدم الرفع
   - ضغط الصور تلقائياً

2. **نظام الإشعارات**
   - إشعارات فورية
   - تنبيهات البريد الإلكتروني
   - إشعارات التحديثات

3. **التقارير والتحليلات**
   - تقارير مفصلة للمطورين
   - رسوم بيانية متقدمة
   - تصدير البيانات (PDF/CSV)

4. **نظام الدفع**
   - دعم التطبيقات المدفوعة
   - تكامل Stripe/PayPal
   - إدارة الاشتراكات

5. **محرك البحث المتقدم**
   - بحث ذكي بالذكاء الاصطناعي
   - اقتراحات تلقائية
   - فلاتر متعددة

6. **وضع الـ PWA**
   - تحويل إلى Progressive Web App
   - العمل بدون اتصال
   - التثبيت على الهواتف

---

## 📞 الدعم والمساعدة

### مشاكل شائعة:

**❓ لا تظهر البيانات:**
- تأكد من إعداد `SUPABASE_URL` و `SUPABASE_ANON_KEY` بشكل صحيح
- تحقق من إنشاء الجداول في Supabase
- افتح Console المتصفح للتحقق من الأخطاء

**❓ لا يمكن تسجيل الدخول:**
- تأكد من تفعيل Email Auth في Supabase
- تحقق من صحة البريد الإلكتروني وكلمة المرور
- تأكد من تأكيد البريد الإلكتروني

**❓ لا يمكن رفع الملفات:**
- تأكد من إنشاء Storage Buckets
- تحقق من سياسات الوصول
- تأكد من حجم الملف (أقل من 100MB)

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - يمكنك استخدامه وتعديله بحرية.

---

## 👨‍💻 المطورون

تم تطوير هذا المشروع بواسطة فريق متخصص في تطوير تطبيقات الويب الحديثة.

---

## 🌟 شكر خاص

- **Bootstrap** - إطار عمل UI رائع
- **Supabase** - قاعدة بيانات سريعة وسهلة
- **Font Awesome** - أيقونات جميلة
- **Chart.js** - رسوم بيانية تفاعلية

---

## 📊 الإحصائيات

- **الصفحات:** 6 صفحات رئيسية
- **ملفات JavaScript:** 5 ملفات
- **ملفات CSS:** 1 ملف رئيسي
- **المكتبات الخارجية:** 5 مكتبات
- **الدعم:** RTL (العربية)

---

**🚀 ابدأ الآن واستمتع ببناء منصتك الخاصة لتوزيع التطبيقات!**
