# تطبيق الدردشة - Chat Application

تطبيق دردشة متكامل يعمل في الوقت الفعلي باستخدام Supabase كقاعدة بيانات وخدمة مصادقة.

## 🌟 المميزات

### ✅ المميزات المكتملة

1. **نظام المصادقة الكامل**
   - تسجيل مستخدم جديد
   - تسجيل الدخول
   - تسجيل الخروج
   - حفظ جلسة المستخدم تلقائياً

2. **واجهة مستخدم حديثة ومتجاوبة**
   - تصميم عصري باستخدام CSS3
   - متوافق مع جميع أحجام الشاشات (موبايل، تابلت، ديسكتوب)
   - رموز Font Awesome
   - خط Cairo للغة العربية

3. **نظام الدردشة المباشرة**
   - إرسال واستقبال الرسائل في الوقت الفعلي (Real-time)
   - عرض قائمة المستخدمين المتاحين
   - عرض آخر رسالة لكل محادثة
   - البحث عن المستخدمين
   - فتح محادثات متعددة

4. **ميزات إضافية**
   - رسائل تنبيه (Toast Notifications)
   - تنسيق الوقت بالعربية
   - حماية من XSS
   - تمرير تلقائي لآخر رسالة

## 🔧 الإعداد والتثبيت

### 1. إنشاء مشروع Supabase

1. اذهب إلى [Supabase](https://supabase.com)
2. أنشئ حساب جديد أو سجل الدخول
3. أنشئ مشروع جديد
4. انتقل إلى Settings > API
5. انسخ:
   - Project URL
   - anon/public key

### 2. إعداد قاعدة البيانات

قم بتنفيذ الاستعلامات SQL التالية في Supabase SQL Editor:

```sql
-- جدول المستخدمين
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  full_name TEXT NOT NULL,
  email TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- جدول الرسائل
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sender_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  receiver_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- إنشاء فهرس لتحسين الأداء
CREATE INDEX idx_messages_sender ON messages(sender_id);
CREATE INDEX idx_messages_receiver ON messages(receiver_id);
CREATE INDEX idx_messages_created_at ON messages(created_at DESC);

-- تمكين Row Level Security
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE messages ENABLE ROW LEVEL SECURITY;

-- سياسات الأمان للمستخدمين
CREATE POLICY "Users can view all profiles" ON profiles
  FOR SELECT USING (true);

CREATE POLICY "Users can insert their own profile" ON profiles
  FOR INSERT WITH CHECK (auth.uid() = id);

CREATE POLICY "Users can update their own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);

-- سياسات الأمان للرسائل
CREATE POLICY "Users can view their own messages" ON messages
  FOR SELECT USING (
    auth.uid() = sender_id OR auth.uid() = receiver_id
  );

CREATE POLICY "Users can insert messages" ON messages
  FOR INSERT WITH CHECK (auth.uid() = sender_id);

-- تمكين Real-time للرسائل
ALTER PUBLICATION supabase_realtime ADD TABLE messages;
```

### 3. تحديث إعدادات التطبيق

افتح ملف `js/config.js` وقم بتحديث القيم التالية:

```javascript
const SUPABASE_URL = 'https://xxxxx.supabase.co'; // ضع رابط مشروعك هنا
const SUPABASE_ANON_KEY = 'your-anon-key-here'; // ضع المفتاح العام هنا
```

### 4. تشغيل التطبيق

1. افتح ملف `index.html` في المتصفح
2. أو استخدم خادم محلي:
   ```bash
   # باستخدام Python
   python -m http.server 8000
   
   # أو باستخدام Node.js
   npx http-server
   ```

## 📁 هيكل المشروع

```
chat-app/
│
├── index.html              # الصفحة الرئيسية
│
├── css/
│   └── style.css          # جميع الأنماط
│
├── js/
│   ├── config.js          # إعدادات Supabase
│   ├── auth.js            # وظائف المصادقة
│   ├── chat.js            # وظائف الدردشة
│   └── main.js            # الوظائف المساعدة
│
└── README.md              # التوثيق
```

## 🎯 كيفية الاستخدام

### للمستخدم الجديد:
1. افتح التطبيق
2. انقر على "إنشاء حساب جديد"
3. أدخل اسمك، بريدك الإلكتروني، وكلمة المرور
4. انقر على "إنشاء حساب"
5. سجل الدخول باستخدام بياناتك

### للمستخدم المسجل:
1. افتح التطبيق
2. أدخل بريدك الإلكتروني وكلمة المرور
3. انقر على "تسجيل الدخول"

### للدردشة:
1. ستظهر لك قائمة المستخدمين في الجانب الأيمن
2. انقر على أي مستخدم لبدء المحادثة
3. اكتب رسالتك في الحقل السفلي
4. اضغط Enter أو انقر على زر الإرسال
5. الرسائل الجديدة تظهر تلقائياً في الوقت الفعلي

## 🔌 واجهات برمجية (API)

التطبيق يستخدم Supabase REST API و Real-time Subscriptions:

### Authentication
- `supabase.auth.signUp()` - إنشاء حساب جديد
- `supabase.auth.signInWithPassword()` - تسجيل الدخول
- `supabase.auth.signOut()` - تسجيل الخروج
- `supabase.auth.getSession()` - الحصول على الجلسة الحالية

### Database Operations
- `supabase.from('profiles').select()` - جلب المستخدمين
- `supabase.from('messages').select()` - جلب الرسائل
- `supabase.from('messages').insert()` - إرسال رسالة

### Real-time Subscriptions
- `supabase.channel().on('INSERT')` - الاستماع للرسائل الجديدة

## 🗃️ نماذج البيانات

### جدول profiles
```javascript
{
  id: UUID,              // معرف المستخدم (من Auth)
  full_name: TEXT,       // الاسم الكامل
  email: TEXT,           // البريد الإلكتروني
  created_at: TIMESTAMP  // تاريخ الإنشاء
}
```

### جدول messages
```javascript
{
  id: UUID,              // معرف الرسالة
  sender_id: UUID,       // معرف المرسل
  receiver_id: UUID,     // معرف المستقبل
  content: TEXT,         // محتوى الرسالة
  created_at: TIMESTAMP  // تاريخ الإرسال
}
```

## 🎨 المكتبات المستخدمة

- **Supabase JS Client** (v2) - قاعدة البيانات والمصادقة
- **Font Awesome** (v6.4.0) - الأيقونات
- **Google Fonts (Cairo)** - الخطوط العربية

## 🚀 الميزات المستقبلية (غير مكتملة)

التطبيق الحالي كامل ويعمل بشكل ممتاز، ولكن يمكن إضافة:

1. **إرسال الصور والملفات**
2. **إشعارات سطح المكتب**
3. **الرسائل الجماعية (Group Chats)**
4. **حالة المستخدم (Online/Offline)**
5. **تشفير الرسائل من طرف إلى طرف**
6. **البحث في الرسائل**
7. **حذف وتعديل الرسائل**
8. **الرموز التعبيرية (Emoji Picker)**
9. **الوضع الليلي (Dark Mode)**
10. **الترجمة لعدة لغات**

## 🐛 استكشاف الأخطاء

### الرسالة: "يرجى تحديث إعدادات Supabase"
- تأكد من تحديث ملف `js/config.js` بمعلومات مشروعك

### لا تظهر الرسائل في الوقت الفعلي
- تأكد من تشغيل Real-time في إعدادات Supabase
- تحقق من أن جدول messages مضاف إلى publication

### خطأ في تسجيل الدخول
- تحقق من صحة البريد الإلكتروني وكلمة المرور
- تأكد من تفعيل حسابك من البريد الإلكتروني (إذا كان مفعلاً في Supabase)

### مشاكل في واجهة المستخدم
- تأكد من تحميل جميع ملفات CSS و JavaScript
- افتح Console في المتصفح للتحقق من الأخطاء

## 📝 ملاحظات مهمة

1. **الأمان**: التطبيق يستخدم Row Level Security (RLS) لحماية البيانات
2. **الأداء**: الفهارس مضافة لتحسين سرعة الاستعلامات
3. **التوافق**: يعمل على جميع المتصفحات الحديثة
4. **اللغة**: الواجهة بالكامل بالعربية مع دعم RTL

## 🔒 الأمان

- استخدام Supabase Auth للمصادقة الآمنة
- Row Level Security (RLS) لحماية البيانات
- تأمين من هجمات XSS
- كلمات المرور مشفرة بواسطة Supabase

## 📧 الدعم

إذا واجهت أي مشاكل أو لديك اقتراحات، يمكنك:
- فتح Issue على GitHub
- مراجعة [توثيق Supabase](https://supabase.com/docs)
- التواصل مع المطور

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم التطوير بواسطة**: AI Assistant  
**تاريخ الإنشاء**: 2025  
**الإصدار**: 1.0.0

## 🎉 ابدأ الآن!

التطبيق جاهز للاستخدام! فقط قم بإعداد Supabase وابدأ المحادثة مع أصدقائك! 🚀
